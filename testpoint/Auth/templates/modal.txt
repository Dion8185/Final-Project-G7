
    <div class="modal fade" id="teacherCodeModal" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content shadow">
                <div class="modal-header bg-gradient-primary text-white">
                    <h5 class="modal-title" id="modalLabel">Teacher Access</h5>
                    <button class="close text-white" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                This form should point to a route that verifies the code
                <form action="{{ url_for('auth.verify_teacher_code') }}" method="POST" class="user">
                    <div class="modal-body text-center">
                        <p class="text-gray-900">Please enter the invitation code</p>
                        <div class="form-group">
                            <input type="text" name="invite_code" class="form-control form-control-user text-center" 
                                placeholder="Enter Invite Code (e.g. TCH-2024)" required>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                        <button class="btn btn-primary" type="submit">Verify & Proceed</button>
                    </div>
                </form>
            </div>
        </div>
    </div>